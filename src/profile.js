const React = require('react'),
      connect = require('react-redux').connect,
      Link = require('react-router').Link

 class Profile extends React.Component {
  render() {
    return <h2>Profile is Me</h2>
  }
}

module.exports  =Profile
